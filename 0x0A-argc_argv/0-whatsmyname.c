#include <stdio.h>
/**
* main - this program prints it's own name
*
* @argc: the number of arguments
*
* @argv: array of arguments
*
* Return: 0
*/
int main(int __attribute__((__unused__)) argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
